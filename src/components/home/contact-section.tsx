const ContactSection = () => {
  return (
    <section
      id="contact"
      className="flex h-screen w-full items-center justify-center border border-cyan-500"
    >
      contact-section
    </section>
  );
};

export default ContactSection;
